{
  "name": "carbon-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/openapi-cli": "^1.0.0-beta.16",
    "gh-pages": "^3.1.0"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs",
    "build": "openapi bundle -o dist",
    "deploy": "gh-pages -d src/build",
    "test": "openapi lint"
  }
}
