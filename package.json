{
  "name": "carbon-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/openapi-cli": "^1.0.0-beta.16",
    "gh-pages": "^3.1.0",
    "core-js": "^3.6.5",
    "mobx": "^4.2.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.3",
    "redoc": "^2.0.0-rc.40",
    "styled-components": "^5.2.0"
  },
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build_dist": "openapi bundle -o dist",
    "build": "react-scripts build",
    "pre_deploy": "build_dist & build",
    "deploy": "gh-pages -d build",
    "test": "openapi lint",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
